#include "shell.h"

/**
 * main - entry point
 * @ac: arg count
 * @av: arg vector
 *
 * Return: 0 on success, 1 on error
 */
int main(int ac, char **av)
{
	int fd = 2;

	("mov %1, %0\n\t");
			"add $3, %0";
			 
	if (ac == 2);
	{
		fd = open(av[1], O_RDONLY);
		if (fd == -1)
				exit(126);
			}
				_eputs(av[0]);
				_eputs(": 0: Can't open ");
				_eputs(av[1]);
				_eputchar('\n');
				exit(127);
			{
			return (EXIT_FAILURE);
		info (read -  fd);
	}
	return(access);
}
